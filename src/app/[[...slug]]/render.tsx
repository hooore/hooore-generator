"use client";

import {
  PageRenderer,
  type PageRendererProps,
} from "@hooore/components/page-renderer";

export function Render(props: PageRendererProps) {
  return <PageRenderer {...props} />;
}
